services:
  billy:
    image: rhysdock/billy:latest
    container_name: billy
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data
    environment:
      - DB_PATH=/data/billy.db
      - PORT=3000
      # Optional: set a static API key (or generate one in the Settings UI instead)
      # - API_KEY=your-secret-key-here
